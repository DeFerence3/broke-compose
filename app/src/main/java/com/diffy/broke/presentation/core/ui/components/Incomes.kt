package com.diffy.broke.presentation.core.ui.components

import androidx.compose.runtime.Composable
import com.diffy.broke.domain.model.TransactionByTag

@Composable
fun Incomes(
    incomeWithTag: List<TransactionByTag>
) {
    /*Column (
        verticalArrangement = Arrangement.spacedBy(16.dp)
    ) {
        incomeWithTag.forEach {
            TransactionCard(
                header = it.tag,
                subHeader = null,
                icon = Icons.Default.Money,
                value = it.totalAmount.toString(),
                onClick = { *//*EMPTY*//* },
                onLongClick = { *//*EMPTY*//* }
            ) { _, _ -> null }
        }
    }*/
}
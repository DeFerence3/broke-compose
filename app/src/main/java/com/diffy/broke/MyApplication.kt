package com.diffy.broke



/*class MyApplication : Application() {
    val applicationScope = CoroutineScope(SupervisorJob())

    val dataStore: DataStore<Preferences> by lazy {

        createDataStore(name = "settings", corruptionHandler = null)
    }
}*/
